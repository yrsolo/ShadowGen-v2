# Schema Package

Path:

- `packages/schema`

Current state:

- placeholder package
- currently contains only `README.md`

Purpose:

- reserved for future standalone schema artifacts when API or integration surface becomes large enough to justify generated or exported schemas outside the main contracts package

Current rule:

- active runtime contracts still live in `packages/contracts`
